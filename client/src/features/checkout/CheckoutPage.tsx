import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant='h3'>
            Only logged users should see this!
        </Typography>
    )
}